{% extends 'layouts/base.twig' %}

{% block content %}
<div class="max-w-7xl mx-auto">
    <h1 class="text-center text-4xl my-8">Регистрация</h1>

    <div class="max-w-1/3 mx-auto">
        <form action="/register" method="post" novalidate>
            <div class="mb-4">
                <input class="input" type="text" name="name" placeholder="Введите имя" value="{{ old('name') }}">
                {% if hasError('name') %}
                    <p class="text-red-600 text-sm mt-1 ml-2">{{ error('name') }}</p>
                {% endif %}
            </div>
            <div class="mb-4">
                <input class="input" type="email" name="email" placeholder="Введите email" value="{{ old('email') }}">
                {% if hasError('email') %}
                    <p class="text-red-600 text-sm mt-1 ml-2">{{ error('email') }}</p>
                {% endif %}
            </div>
            <div class="mb-4">
                <input class="input" type="password" name="password" placeholder="Введите пароль">
                {% if hasError('password') %}
                    <p class="text-red-600 text-sm mt-1 ml-2">{{ error('password') }}</p>
                {% endif %}
            </div>
            <button class="btn w-full">Отправить</button>
        </form>
    </div>

    <div class="text-center text-sm mt-4">
        <p>Если у вас есть аккаунт, <a class="text-red-500" href="/news.kg/login">авторизуйтесь</a>.</p>
    </div>
</div>
{% endblock %}