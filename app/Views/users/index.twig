{% extends "layouts/base.twig" %}

{% block content %}
    <h1>Пользователи</h1>
    <ul>
        {% for user in users %}
            <li>
                <a href="/users/{{ user.id }}">{{ user.name }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
